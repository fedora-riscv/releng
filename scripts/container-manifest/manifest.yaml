image: {registry}.fedoraproject.org/{image}:{tag}
tags: ['{tag}', '{tag-name}']
manifests:
 - image: {registry}.fedoraproject.org/{image}:{tag}-armhfp
   platform:
     architecture: arm
     variant: v7
     os: linux
 -
   image: {registry}.fedoraproject.org/{image}:{tag}-aarch64
   platform:
     architecture: arm64
     variant: v8
     os: linux
 -
   image: {registry}.fedoraproject.org/{image}:{tag}-x86_64
   platform:
     architecture: amd64
     os: linux
 -
   image: {registry}.fedoraproject.org/{image}:{tag}-ppc64le
   platform:
     architecture: ppc64le
     os: linux
 -
   image: {registry}.fedoraproject.org/{image}:{tag}-s390x
   platform:
     architecture: s390x
     os: linux
